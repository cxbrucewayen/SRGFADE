# Tests

The test suite covers self-contained fixed-seed numeric regression and the
public minimization interface. The frozen regression observations were
recorded after comparison with the authoritative implementation and do not
depend on files outside this repository.
